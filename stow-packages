# paths (relative to .dotfiles) that `just stow .stow-packages` should
# symlink as a single whole file/directory, rather than the default
# `--no-folding` per-file linking that the plain `just stow` uses.
#
# one path per line, lines starting with # are ignored.
# format: source [-> target]  (target defaults to source, both relative
# to .dotfiles / $HOME respectively)
Claude.md
.agents
.zsh_plugins
.bash_profile
.bashrc
.editorconfig
.gitconfig
.gitflow_export
.gitmodules
.hammerspoon
.homebrew
.tokensave
.vimrc
.vscode
.zprofile
.zshenv
.zshrc
.zsh_plugins.txt
.zsh_plugins.zsh
.claude/settings.json -> .claude/settings.json
.codex/config.toml -> .codex/config.toml
.openclaw
.openwolf
gh -> .config/gh
git -> .config/git
github-copilot -> .config/github-copilot
homebrew -> .config/homebrew
iterm2 -> .config/iterm2
zed -> .config/zed
zsh -> .config/zsh