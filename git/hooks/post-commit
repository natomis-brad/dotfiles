#!/bin/sh
# tokensave: chain-repo-hook
repo_hook="$(git rev-parse --git-dir 2>/dev/null)/hooks/post-commit"
if [ -x "$repo_hook" ] && [ "$repo_hook" != "$0" ]; then
	"$repo_hook" "$@"
fi

# tokensave: auto-sync
/opt/homebrew/bin/tokensave sync >/dev/null 2>&1 &
